
void fenToBoardState(char * fen) //, BSTATE *b)
{
	char* whitePieces;
	char* blackPieces; 
	while((blackPieces = getc(fen)) != '/' && c != EOF)
	{
		printf("%c \n", blackPieces);
	}
}

int main()
{
	fentoBoardState("rnbqkbnr/")
	return 0;
}
